Menu="Tasks:59"
Type="xmenu"
Name="StaXX"
Title="StaXX"
Tag="staxx.png"
Code="f1b3"
Lock="true"
Markdown="false"
Cond="($var['fsState'] ?? '') == 'Started' && is_file('/var/run/dockerd.pid') && (file_exists('/boot/config/plugins/staxx/header_menu') || file_exists('/boot/config/plugins/staxx/takeover_docker_tab'))"
---
<?PHP
/* StaXX — the app's own button in the top navigation bar.
 * Copyright 2026, StaXX contributors.
 *
 * Rank 59 seats this immediately left of the stock Docker button (Tasks:60).
 *
 * THREE separate things name this page, and they are read from three places:
 *
 *   Name=      the text on the button. Unraid's navigation prints Name and
 *              falls back to the FILENAME when it is absent — Title is never
 *              consulted for it, which is why setting Title alone left the
 *              button reading "Stacks".
 *   Title=     the page's own header bar, above the content.
 *   filename   the URL. PageBuilder sets a page's name to its basename, so
 *              this file being StaXX.page is what makes the address /StaXX.
 *
 * The filename is also why this file and the Docker sub-tab cannot both be
 * called StaXX.page. That one is Stacks.page, giving /Docker/Stacks, and only
 * one of the two is ever live.
 *
 * Enabled when the user has opted into header placement, or when the Docker
 * menu has been taken over — with the takeover on, the Docker button and
 * every sub-tab under it (including Stacks.page) are gone, so if
 * HEADER_MENU were also off there would be no way into StaXX at all.
 * The takeover therefore forces this button on regardless of HEADER_MENU.
 *
 * This program is free software; you can redistribute it and/or
 * modify it under the terms of the GNU General Public License version 2,
 * as published by the Free Software Foundation.
 */
?>
<? include '/usr/local/emhttp/plugins/staxx/include/StacksPage.php'; ?>
