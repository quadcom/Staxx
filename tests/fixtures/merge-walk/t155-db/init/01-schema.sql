CREATE TABLE greetings (id INT AUTO_INCREMENT PRIMARY KEY, text VARCHAR(80) NOT NULL);
INSERT INTO greetings (text) VALUES
  ('Hello from MariaDB'),
  ('If you can read this the wiring survived'),
  ('Third row, so the count is not one');
